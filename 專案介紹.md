# mathced-com/teach

## 專案目的

集中保存教師可直接在瀏覽器使用的班級管理、課堂抽籤、互動學習、心智圖與圖片處理工具，方便以 GitHub Pages 或本機靜態網頁方式使用。

## 使用對象

- 教師：班級管理、作業檢核、行事曆、座位與抽籤工具。
- 學生：數學、國語及課堂互動學習活動。

## 目前範圍

- 以 `index.html` 作為主要入口。
- 班級行事曆、掃地檢查、備忘錄與作業追蹤。
- 多區及全班座位抽籤、幸運大轉盤。
- 最大公因數、最小公倍數、分數除法、小數除法及《論語》互動練習。
- 心智圖編輯、匯入／匯出、簡報與雲端同步。
- 貼圖提示詞產生與圖片網格裁切。

## 不包含的範圍

- Google Apps Script、試算表及雲端硬碟等後端服務的原始碼與部署管理。
- 統一的帳號、權限或學生資料庫系統。
- Node.js 或其他框架的建置流程；目前以靜態 HTML、CSS、JavaScript 為主。

## 目前狀態

- 階段：維護與整理。
- 最近成果：移除不再需要的舊版頁面，並將首頁改為科技教學風格；目前 17 個功能頁均已分類並加入入口連結。
- 下一個里程碑：為保留的主要工具建立一致的操作驗證清單。

## 主要技術或工具

- HTML、CSS、JavaScript。
- Tailwind CSS、React、jsMind、SheetJS、html2canvas、jsPDF、JSZip 等瀏覽器端函式庫。
- Google Apps Script 作為部分工具的資料讀寫後端。
- GitHub 作為版本控制與發布來源。

## 卡片素材規則

- `卡片素材/高解析/` 保存原始 PNG，供放大、編輯與印刷使用。
- `卡片素材/低解析/` 保存 480×670 WebP，供 GitHub Pages 與網路載入使用。
- `classroom-reward-system.html` 一律引用低解析 WebP；`classroom-reward-system-portable.html` 使用低解析 WebP 的內嵌資料。
- 目前未提供卡片圖片上傳功能；日後若加入，必須先將上傳檔轉成低解析 WebP 再儲存。

## 如何開始

1. 先讀取 `AGENTS.md`。
2. 再讀取 `工作筆記.md` 最新一筆。
3. 檢查 `git status --short --branch`，確認工作區狀態。
4. 由 `index.html` 開始檢查網站入口；需測試特定工具時，直接開啟對應 HTML。
5. 依工作筆記的「下一步」接續工作。

## 重要連結

- GitHub：`https://github.com/mathced-com/teach`
- Obsidian 鏡像：未設定。
